[secrets]
wifi_ssid=
wifi_password=
# Порт заливки выбирается по USB-серийнику платы (`pio device list` показывает
# его как SER=), потому что имя /dev/cu.* принадлежит разъёму, а не плате.
# Нужно, только когда к машине подключено несколько плат. См. docs/build.md.
#[board_serial]
#esp32-c6-super-mini = REDACTED
#nodemcuv2 = 0001
